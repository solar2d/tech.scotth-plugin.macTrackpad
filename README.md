Sources for the plugin `plugin.macTrackpad`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.macTrackpad" = {
            publisherId = "tech.scotth",
        },
    },
}
```
